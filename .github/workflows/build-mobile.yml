name: Build Mobile Apps (Android & iOS)

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
      - '.gitignore'
  pull_request:
    branches: [ main, master ]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
      - '.gitignore'
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK & AAB
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Setup Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Node Dependencies
        run: npm install

      - name: Build Web Application & PWA Assets
        run: npm run build

      - name: Sync Capacitor Android Platform
        run: npx cap sync android

      - name: Make Gradle Wrapper Executable
        run: chmod +x android/gradlew

      - name: Build Android Debug APK & Release Bundle
        run: |
          cd android
          ./gradlew assembleDebug bundleRelease --no-daemon

      - name: Upload Android Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: Workflare-Invoicing-Android-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload Android Release AAB Bundle
        uses: actions/upload-artifact@v4
        with:
          name: Workflare-Invoicing-Android-AAB
          path: android/app/build/outputs/bundle/release/app-release.aab

      - name: Publish Android Binaries to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v1.0.0"
          files: |
            android/app/build/outputs/apk/debug/app-debug.apk
            android/app/build/outputs/bundle/release/app-release.aab
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-ios:
    name: Build iOS Xcode App Package
    needs: build-android
    permissions:
      contents: write
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Setup Xcode Version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install Node Dependencies
        run: npm install

      - name: Build Web Application & PWA Assets
        run: npm run build

      - name: Sync Capacitor iOS Platform
        run: npx cap sync ios

      - name: Build iOS Device Binary (iphoneos ARM64)
        run: |
          cd ios/App
          xcodebuild -project App.xcodeproj -scheme App -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO

      - name: Package Real Device iOS (.ipa) Package
        run: |
          cd ios/App/build/Build/Products/Release-iphoneos
          mkdir -p Payload
          cp -r App.app Payload/
          zip -r WorkflareInvoicing-iOS.ipa Payload

      - name: Upload iOS (.ipa) App Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Workflare-Invoicing-iOS-IPA
          path: ios/App/build/Build/Products/Release-iphoneos/WorkflareInvoicing-iOS.ipa

      - name: Publish iOS Binaries to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v1.0.0"
          files: ios/App/build/Build/Products/Release-iphoneos/WorkflareInvoicing-iOS.ipa
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
